const [tags, setTags] = useState(["happy", "exciting"]);


const handleClick = () => {
//For add
setTags ({...tags, "Cheerful"}) 

//For remove
setTags ({tags.filter(tag => tags !== "Happy")}) in yani hame b qeyre happy

//For update
setTags (tags.map(tag => tag === 'happy' ? 'happiness' : tag))
}



Optimistic Update  /* I prefer this to use */
{
    update the ui
    call the server
}


Pessimistic Update
{
    call the server
    update the ui
}
